// on-click listener for log in
$("").on("click", () => {
    // reroute to initial quiz or results page
})

// on-click listener for sign up
$("").on("click", () => {
    // reroute to initial quiz
})